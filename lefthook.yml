glob_matcher: doublestar

pre-commit:
  parallel: false
  commands:
    prettier:
      glob: "*.{js,jsx,ts,tsx,mjs,cjs,json,jsonc,yaml,yml,md,css,html}"
      exclude:
        - "**/dist/**"
        - "**/dist-stdio/**"
        - "**/out/**"
        - "**/build/**"
        - "**/coverage/**"
        - "**/.worktrees/**"
        - "**/.intellijPlatform/**"
        - "**/.vscode-test-web/**"
      run: npx prettier --write {staged_files}
      stage_fixed: true
    eslint:
      glob: "*.{js,jsx,ts,tsx,mjs,cjs}"
      exclude:
        - "**/dist/**"
        - "**/dist-stdio/**"
        - "**/out/**"
        - "**/build/**"
        - "**/coverage/**"
        - "**/.worktrees/**"
        - "**/.intellijPlatform/**"
        - "**/.vscode-test-web/**"
      run: npx eslint --fix {staged_files}
      stage_fixed: true
    kotlin-format:
      glob: "*.{kt,kts}"
      run: ./apps/jetbrains/gradlew -p apps/jetbrains spotlessCheck

pre-push:
  commands:
    quality:
      # Without a JDK the JetBrains stage is skipped rather than blocking the push; CI
      # runs it either way. `java -version` and not `command -v java`, because macOS
      # ships a /usr/bin/java stub that exists with no runtime behind it.
      run: |
        if [ -n "$JAVA_HOME" ] || java -version >/dev/null 2>&1; then
          npm run check
        else
          echo "lefthook: no JDK found, skipping check:jetbrains — CI will run it" >&2
          npm run check:typescript && npm run check:docs
        fi
