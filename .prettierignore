# GEDCOM validation schemas. g7validation.json is generated by
# `npm run generate -w packages/validator` and must never be hand-edited; g551validation.json
# is hand-maintained but is a quarter-megabyte of data, and reformatting it would bury real
# changes under whitespace. Both stay in the format their tooling produces.
packages/validator/src/schemes/g551validation.json
packages/validator/src/schemes/g7validation.json
