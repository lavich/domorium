/tmp
/out-tsc
/coverage

/node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
/.pnp
.pnp.js

.vscode/*
.idea/*

/.worktrees

# Playwright MCP session artifacts
/.playwright-mcp

# The shared agent hook belongs to the project; a personal override does not.
# A global excludes file may hold `**/.claude/*`, which is why the first line
# re-includes rather than merely omitting.
!.claude/settings.json
.claude/settings.local.json
